# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android Studio / IntelliJ
*.iml
.idea/
local.properties

# APK / AAB
*.apk
*.aab

# Keystore (never commit signing keys)
*.keystore
*.jks
# Exception: fixed debug-only keystore, checked in on purpose so CI builds
# share one signature and updates install in place (see build-debug.yml).
!/debug.keystore

# macOS
.DS_Store
._*

# Credentials (if accidentally placed here)
.pulse-credentials.json
